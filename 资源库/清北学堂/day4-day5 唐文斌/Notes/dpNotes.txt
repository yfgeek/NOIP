
// 改变计算顺序

for i := ...
	// [1..i-1]

	把A1..Ai-1排序, 键值相等，位置靠前的优先
	nlogn
	
	for j := 1 .. 
		//最大的k 满足 Ak = Aj - Ai

		// 找Ak:  二分查找? --> O(logN)

		F[i, j] = ...



for i := ...
	// [1..i-1]

	//把A1..Ai-1排序, 键值相等，位置靠前的优先
	
	for j := i+1 -> N

	将Ai+1 .. An也从小到大排序, 按照这个顺序枚举j


		//最大的k 满足 Ak = Aj - Ai

		// 找Ak:  二分查找? --> O(logN)

		F[i, j] = ...